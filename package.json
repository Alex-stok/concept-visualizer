{
  "name": "concept-visualizer",
  "version": "0.1.0",
  "description": "A concept exploration of an audio-reactive visualizer for track players — real Web Audio analysis driving 3 canvas-based visual styles.",
  "type": "module",
  "main": "dist/av-visualizer.js",
  "files": [
    "dist",
    "src"
  ],
  "license": "UNLICENSED",
  "author": "Alex-stok",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "build": "node scripts/build.mjs",
    "prepare": "node scripts/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
